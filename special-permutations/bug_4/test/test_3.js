
var { specialPerm  } = require('../solution');
var assert = require('assert');
  describe('test_3', function () {
    it('For input nums = [2,4], the result should be 2', function () {
      assert.deepEqual( specialPerm( [2,4]) , 2);
    });
  });
    